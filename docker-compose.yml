services:
  fileupload-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fileupload-expressjs
    volumes:
      - ./images:/app/file
    ports:
      - "8020:3000"
    environment:
      - NODE_ENV=production